<%= render :partial => 'layouts/header', :locals => {:title => yield(:title).to_s} %>

<div id="container">
	<% flash.each do |name,msg| %>
		 <%= content_tag :div, msg, :id => "flash_#{name}", :class => "users_flash" %>
	<% end %>
    <div class="container-users">
		<%= yield %>
    </div>
</div>

<%= render :partial => 'layouts/footer' %>
